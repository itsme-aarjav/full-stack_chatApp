apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-mongodb

spec:
  clusterIP: None

  selector:
    app: {{ .Release.Name }}-mongodb

  ports:
    - port: {{ .Values.mongodb.service.port }}
      targetPort: 27017

