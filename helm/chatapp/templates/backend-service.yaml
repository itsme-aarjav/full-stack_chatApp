apiVersion: v1
kind: Service

metadata:
  name: backend

spec:
  type: {{ .Values.backend.service.type }}

  selector:
    app: {{ .Release.Name }}-backend

  ports:
    - protocol: TCP
      port: {{ .Values.backend.service.port }}
      targetPort: 5001

